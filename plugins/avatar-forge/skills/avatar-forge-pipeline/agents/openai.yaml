interface:
  display_name: "Avatar Forge"
  short_description: "Rewrite reference videos or create avatar videos"
  default_prompt: "Use $avatar-forge-pipeline to transcribe and rewrite my reference video, then create a final zeroshot avatar video if I ask."
