interface:
  display_name: "Avatar Forge"
  short_description: "Compose avatar, lip-sync, voice, and video tasks"
  default_prompt: "Use $avatar-forge-pipeline to choose only the digital-human capabilities I need and guide me through the required inputs."
