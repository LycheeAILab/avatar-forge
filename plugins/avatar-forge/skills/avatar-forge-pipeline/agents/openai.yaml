interface:
  display_name: "Avatar Forge"
  short_description: "Create a final zeroshot talking-avatar video"
  default_prompt: "Use $avatar-forge-pipeline with LycheeTTS through LycheeAILab to create my final talking-avatar video and return only the zeroshot MP4, never an internal template."
